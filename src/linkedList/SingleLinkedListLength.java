package linkedList;

/**
 * Created by dgup27 on 4/22/2016.
 */
public class SingleLinkedListLength {
    /*public int length(){
        int count=0;
        Node current = this.head;

        while(current != null){
            count++;
            current=current.next()
        }
        return count;
    }

    public int length(Node current){
        if(current == null) //base case
            return 0;

        return 1+length(current.next());
    }*/


}
